# APD 完整数据集生成说明

## 📊 数据规模

### BBQ Disability 完整数据
- **总样本数**: 1,556 条
- **数据分布**:
  - `disambig_neg`: 389 条
  - `disambig_nonneg`: 389 条
  - `ambig_neg`: 389 条
  - `ambig_nonneg`: 389 条

### 子类别分布
- **Physical (身体残疾)**: 432 条
- **MentalIllness (精神疾病)**: 520 条

### 数据集划分
- **训练集**: 1,086 条 (70%)
- **验证集**: 230 条 (15%)
- **测试集**: 240 条 (15%)
- **总计**: 1,556 条

### 增强后规模（2倍）
- **训练集**: 2,172 条
- **验证集**: 230 条
- **测试集**: 240 条
- **总计**: 2,642 条

---

## 🚀 使用步骤

### 步骤1: 快速启动（已完成）
```bash
python quick_start.py
```
**功能**: 加载和划分数据
**状态**: ✅ 已完成
**输出**:
- `apd_results_full/train_full.json`
- `apd_results_full/val_full.json`
- `apd_results_full/test_full.json`
- `apd_results_full/dataset_stats.json`

### 步骤2: 生成提示词（分阶段运行）

#### 选项A: 完整运行（自动断点续传）
```bash
python run_full_pipeline.py all
```
**预计时间**: 3-4小时
**特点**: 自动检测已完成的阶段，支持断点续传

#### 选项B: 从阶段2开始运行
```bash
python run_full_pipeline.py from_stage 2
```
**功能**: 从生成训练集提示词开始

#### 选项C: 单独运行各阶段
```bash
# 阶段2: 生成训练集提示词（约2-3小时）
python run_full_pipeline.py from_stage 2

# 阶段3: 生成验证集提示词（约30-45分钟）
python run_full_pipeline.py from_stage 3

# 阶段4: 生成测试集提示词（约30-45分钟）
python run_full_pipeline.py from_stage 4

# 阶段5: 数据增强（约10分钟）
python run_full_pipeline.py from_stage 5
```

### 步骤3: 数据增强（可选）
```bash
python augment_dataset.py
```
**功能**: 对训练集进行2倍增强
**输出目录**: `apd_results_full_augmented/`

---

## 📁 输出文件结构

### 原始数据
```
apd_results_full/
├── train_full.json          # 原始训练集 (1,086条)
├── val_full.json            # 原始验证集 (230条)
├── test_full.json           # 原始测试集 (240条)
└── dataset_stats.json       # 统计信息
```

### SFT数据（生成后）
```
apd_results_full/
├── sft_train_full.json      # SFT训练数据 (1,086条)
├── sft_val_full.json        # SFT验证数据 (230条)
├── sft_test_full.json       # SFT测试数据 (240条)
└── sft_data_full.json       # 完整SFT数据 (1,556条)
```

### 增强后数据
```
apd_results_full_augmented/
├── sft_train_augmented.json # 增强训练集 (2,172条)
├── sft_val.json             # 验证集 (230条)
├── sft_test.json            # 测试集 (240条)
└── sft_data_all.json        # 完整数据 (2,642条)
```

---

## ⚙️ 配置说明

### API配置
- **端点**: Azure OpenAI
- **模型**: DeepSeek-V3.2
- **API Key**: 已配置在脚本中

### 数据划分配置
```python
# 划分比例
train_ratio = 0.7    # 70% 训练
val_ratio = 0.15     # 15% 验证
test_ratio = 0.15    # 15% 测试

# 随机种子
random_seed = 42
```

### 增强配置
```python
# 增强倍数
augmentation_factor = 2  # 训练集翻倍
```

---

## ⏱️ 时间估算

| 阶段 | 时间 | 说明 |
|------|------|------|
| 阶段1 | 1分钟 | 加载和划分数据 ✅ |
| 阶段2 | 2-3小时 | 生成训练集提示词 (1,086条 × 5秒) |
| 阶段3 | 30-45分钟 | 生成验证集提示词 (230条 × 5秒) |
| 阶段4 | 30-45分钟 | 生成测试集提示词 (240条 × 5秒) |
| 阶段5 | 10分钟 | 数据增强 |
| **总计** | **3.5-4.5小时** | 完整流程 |

---

## 💰 成本估算

- **API调用次数**: ~1,556 次（提示词生成）
- **每次调用成本**: ~0.01元
- **总成本**: ~15-20元

---

## 🔄 断点续传

如果运行中断，可以使用以下命令恢复：

```bash
# 查看当前进度
cat apd_results_full/pipeline_checkpoint.json

# 从断点继续
python run_full_pipeline.py all

# 或从指定阶段继续
python run_full_pipeline.py from_stage <阶段号>
```

---

## 📋 数据格式

### SFT数据格式
```json
{
  "messages": [
    {
      "role": "system",
      "content": "你是一个专门生成反偏见提示词的助手..."
    },
    {
      "role": "user",
      "content": "Context: ...\nQuestion: ...\n\n请生成一个反偏见提示词..."
    },
    {
      "role": "assistant",
      "content": "生成的反偏见提示词"
    }
  ],
  "context": "原始上下文",
  "question": "原始问题",
  "anti_bias_prompt": "生成的反偏见提示词",
  "original_record": {原始BBQ记录}
}
```

---

## ✅ 下一步操作

现在你可以：

1. **开始生成提示词**:
   ```bash
   python run_full_pipeline.py all
   ```

2. **或分阶段运行**（推荐）:
   ```bash
   python run_full_pipeline.py from_stage 2
   ```

3. **监控进度**:
   - 每10条数据会保存临时文件
   - 检查点文件记录运行状态
   - 控制台输出实时进度

---

## ⚠️ 注意事项

1. **网络稳定**: 确保网络连接稳定，避免API调用失败
2. **API限额**: 注意Azure API的调用限额
3. **存储空间**: 确保有足够的磁盘空间 (~50MB)
4. **运行时间**: 建议在空闲时间运行，避免中断

---

## 📞 问题排查

### 问题1: API调用失败
**解决**: 检查网络连接，脚本会自动重试3次

### 问题2: 内存不足
**解决**: 分阶段运行，减少同时处理的数据量

### 问题3: 运行中断
**解决**: 使用 `run_full_pipeline.py all` 自动恢复

---

## 🎯 预期结果

运行完成后，你将获得：

1. **完整SFT数据集**: 1,556 条（或增强后 2,642 条）
2. **训练/验证/测试划分**: 70/15/15
3. **反偏见提示词**: 为每个样本生成
4. **统计信息**: 详细的数据分布报告

这些数据可以直接用于微调你的反偏见提示词生成模型！
